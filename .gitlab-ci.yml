stages:
  - deploy

production_server_1:
  stage: deploy
  tags:
    - tag-hare-krishna-backend-prod
  script:
    - rsync -hravz --exclude '.git' --exclude '.well-known' --exclude 'media' --exclude '.gitlab-ci.yml' --exclude '.env' . /var/www/erp-backend/html
    - chown -R root:root /var/www/erp-backend/html

production_server_2:
  stage: deploy
  tags:
    - tag-hare-krishna-backend-sync-prod
  script:
    - rsync -hravz --exclude '.git' --exclude '.well-known' --exclude 'media' --exclude '.gitlab-ci.yml' --exclude '.env' . /var/www/hkisyncapi/html
    - chown -R root:root /var/www/hkisyncapi/html